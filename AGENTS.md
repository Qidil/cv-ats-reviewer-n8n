# AGENTS.md

## Running Processes

- If a backend or any process is started in a terminal that the user cannot
  manually stop (e.g. background `Start-Process`, detached servers, watchers),
  **shut it down when it is no longer needed.** Do not leave orphan processes.
